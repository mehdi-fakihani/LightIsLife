using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace LIL
{
    /// <summary>
    /// List all actors available. They are assigned in Unity's editor.
    /// Add a new value here to have an id available for a new actor in the editor.
    /// </summary>
    public enum ActorsID {
        Hero,
        Slime,
    }
}
