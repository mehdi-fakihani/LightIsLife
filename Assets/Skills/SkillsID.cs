using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace LIL
{
    /// <summary>
    /// List all skills available. They are assigned in Unity's editor.
    /// Add a new value here to have an id available for a new skill in the editor.
    /// </summary>
    public enum SkillsID
    {
        HeroAttack,
        SlimeAttack,
        Fireball,
    }
}
