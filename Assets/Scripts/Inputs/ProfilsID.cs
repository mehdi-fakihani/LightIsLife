using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace LIL.Inputs
{
    /// <summary>
    /// List all profile models available. They are assigned in Unity's editor.
    /// Add a new value here to have an id available for a new profile model in the editor.
    /// </summary>
    public enum ProfilsID
    {
        KeyboardAZERTY,
        KeyboardQWERTY,
        XBoxGamepad
    }
}
